version: "2"
services:
  CIBuilder:
    build: ./
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
    volumes:
      - ../../:/home/node/app
    expose:
      - "8081"
    command: "mocha /home/node/app/tests"
version: "2"
services:
  cibuilder:
    build: ./
    user: "root"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=test
    volumes:
      - ../../:/home/node/app
    command: /bin/bash build/CI/build.sh